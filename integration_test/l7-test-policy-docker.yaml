tailscale:
  service: "tailvoy-l7-test"
  clientId: "${TS_CLIENT_ID}"
  clientSecret: "${TS_CLIENT_SECRET}"
  tags:
    - "tag:docker"
  serviceTags:
    - "tag:docker"

listeners:
  - name: web
    protocol: tcp
    listen: ":80"
    forward: "backend:8080"
    l7_policy: true
